Scroll-up fixed header

JavaScript를 이용하여, 사이트의 헤더가 스크롤 다운시엔 보이지 않았다가, 다시 스크롤 업을 할 시에 헤더가 노출이 되도록 구현했습니다.

또한 해상도에 따라 적절하게 레이아웃이 변하게 구성되어있습니다.

- JavaScript 이용

- header의 위치가 scroll-up시 상단에 고정되게 구현

- 반응형 CSS 처리


Mobile GNB

JavaScript를 이용하여, 모바일 환경에서도 GNB 메뉴가 원활하게 작동하게 구현되어있습니다.

또한 해상도에 따라 적절하게 레이아웃이 변하게 구성되어있습니다.

- JavaScript 이용

- 모바일 해상도에서도 적절한 인터페이스 제공

- 반응형 CSS 처리


Client Data - Cookie Popup

Local storage의 cookie를 이용하여, 팝업창을 구현하였습니다.

- JavaScript 이용

- Client Data의 Local storage 사용

- 반응형 CSS 처리


SwiperJS Basic

SwiperJS 라이브러리를 이용하여, 슬라이더의 이전, 다음 버튼과 재생 시작과 멈춤을 조절할 수 있는 버튼을 구현하였습니다.

- JavaScript 이용

- SwiperJs 라이브러리 사용

- play, pause, prev, next 버튼 구현



Responsive SwiperJS

SwiperJS 라이브러리를 이용하여, PC 화면에선 일반적으로 표시, 모바일 환경에선 Swiper 형태로 작동하게 구현하였습니다.

- JavaScript 이용

- SwiperJs 라이브러리 사용

- 모바일 환경에서만 Swiper로 구동되게 구현


SwiperJS to CSS transition

SwiperJS 라이브러리와 CSS 속성을 이용하여 부드러운 애니메이션으로 작동하는 Swiper를 구현하였습니다.

- JavaScript 이용

- SwiperJs 라이브러리 사용

- Swiper와 CSS 속성을 이용한 애니메이션 구현